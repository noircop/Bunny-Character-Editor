init python:
    BCE_ROOT = "modules/bunny_character_editor/"

    BCE_ASSETS = BCE_ROOT + "assets/"
    BCE_CHAR_PATH = BCE_ASSETS + "character/"
    BCE_UI_COMPONENTS = BCE_ASSETS + "ui/components/"

    # character preview config
    BCE_PREVIEW_ZOOM = 0.5
